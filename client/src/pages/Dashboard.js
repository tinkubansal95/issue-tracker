import React from "react";

const Dashboard = () => {
  return (
    <div className="container">
      <div>
        <button class="ui right floated button">Add New Issue</button>
      </div>
    </div>
  );
};

export default Dashboard;
